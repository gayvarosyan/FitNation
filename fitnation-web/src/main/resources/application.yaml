spring:
  application:
    name: fitnation-web
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
  datasource:
    url: jdbc:postgresql://localhost:5432/fitnation
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: adminfitnation@gmail.com
    password: ukox vxgg dtvi vkhp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  secret: fitnation-super-secret-key-must-be-at-least-32-characters-long
  expiration: 3600
  refresh-expiration: 604800

fitnation:
  app:
    login-url: http://localhost:8080/login
