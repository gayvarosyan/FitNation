package com.example.fitnationcommon.enums;

public enum ErrorCode {
    VALIDATION_FAILED,
    INVALID_ARGUMENT,
    INVALID_FILTER,
    NOT_FOUND,
    CONFLICT,
    FORBIDDEN,
    UNAUTHORIZED,
    INTERNAL_ERROR
}