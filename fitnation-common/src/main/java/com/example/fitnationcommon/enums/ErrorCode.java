package com.example.fitnationcommon.enums;

public enum ErrorCode {
    VALIDATION_FAILED,
    INVALID_ARGUMENT,
    INVALID_FILTER,
    NOT_FOUND,
    CONFLICT,
    FORBIDDEN,
    UNAUTHORIZED,
    INTERNAL_ERROR,
    ACCESS_DENIED,
    ROLE_NOT_ALLOWED,
    OWNERSHIP_REQUIRED
}
